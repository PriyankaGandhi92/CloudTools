rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to read/write PDF files
    match /pdfs/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
